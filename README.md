

# Вот и переделай все твое сообщение выше в один README пожалуйста

```markdown
# Mindmap TG Bot

Telegram‑бот, который помогает быстро понять суть документов: ты загружаешь файл или фото, бот распознаёт текст, строит структуру через LLM и показывает интеллект‑карту (mindmap) в виде мини‑приложения.[file:1]

## Основные возможности

- Принимает:
  - фотографии документов (JPEG/PNG);[file:1]
  - текстовые файлы (TXT/MD/CSV);
  - DOCX;
  - PPTX;
  - PDF (текстовые и сканы).
- Распознаёт текст:
  - через Yandex Cloud OCR для изображений и сканов;[file:1]
  - через локальные парсеры для текстовых форматов (pypdf, python-docx, python-pptx).
- Строит структурированную карту документа через Yandex LLM по заранее заданному JSON‑формату.[file:1]
- Сохраняет карту в Object Storage (S3‑совместимый бакет) как Markdown для Markmap.[file:1]
- Показывает результат:
  - как текстовую структуру в Telegram;
  - как интерактивную mindmap в mini‑app (webapp/index.html).[file:1]
- Ведёт историю карт для каждого пользователя (in‑memory по user_id).[file:1]

## Технологии

- Backend: Python, aiogram 3 (Telegram Bot API).[file:1]
- LLM & OCR: Yandex Cloud (Foundation Models, Vision OCR).[file:1]
- Хранилище: Yandex Object Storage (S3 API) через `boto3`.[file:1]
- Frontend (mini‑app): Telegram WebApp, Markmap (`markmap-lib`, `markmap-view`).[file:1]

## Структура проекта

```text
config.py/          # загрузка настроек из .env
main.py/            # точка входа, регистрация роутеров

handlers/           # логика Telegram-бота
  start.py/         # /start, приветствие и главное меню
  menu.py/          # текстовое меню, "Создать карту", "История"
  upload.py/        # приём файла/фото от пользователя
  settings.py/      # название карты, глубина анализа, выбор модели
  process.py/       # OCR + LLM + сохранение карты + ответы
  history.py/       # вывод истории карт
  cancel.py/        # /cancel, сброс сценария
  view_map.py/      # показ структуры карты в виде текста

services/           # работа с внешними сервисами и бизнес-логика
  document_text.py/ # извлечение текста из фото и файлов (OCR + парсинг)
  llm.py/           # вызов Yandex LLM и генерация структуры/Markmap
  storage.py/       # in-memory хранилище карт + загрузка Markdown в S3
  yandex_storage.py/# (опционально) загрузка HTML в Object Storage
  progress.py/      # обновление статус-сообщений

keyboards.py/       # все клавиатуры (главное меню, глубина, история)
states.py/          # FSM-состояния (CreateMap, Global)

webapp/index.html/  # мини-приложение для отображения mindmap
```

> Некоторые файлы могут быть черновиками/заглушками и не участвовать в основном сценарии (например, тестовые скрипты и старые версии сервисов).[file:1]

## Установка и запуск

### 1. Установить зависимости

Создай виртуальное окружение и файл `requirements.txt` со строками:

```txt
aiogram==3.4.1
python-dotenv==1.0.1

requests==2.32.3
boto3==1.35.0

pypdf==5.0.0
python-docx==1.1.0
python-pptx==1.0.2

mimetypes-magic==0.5.0
```

Установи зависимости:

```bash
pip install -r requirements.txt
```


### 2. Настроить переменные окружения

Создай `.env` в корне:

```env
BOT_TOKEN=telegram_bot_token

YANDEX_API_KEY=yc_api_key
YANDEX_FOLDER_ID=yc_folder_id
YANDEX_URL=ds://your-llm-model-uri
YANDEX_API_URL=https://llm.api.cloud.yandex.net/foundationModels/v1/completion
YANDEX_OCR_URL=https://ocr.api.cloud.yandex.net/ocr/v1/recognizeText

YC_ACCESS_KEY_ID=access_key_id
YC_SECRET_ACCESS_KEY=secret_access_key
YC_BUCKET_NAME=webapp-tgmini
YC_S3_ENDPOINT=https://storage.yandexcloud.net
YC_WEBSITE_HOST=webapp-tgmini.website.yandexcloud.net
```

- `BOT_TOKEN` — токен бота из `@BotFather`.[file:1]
- Параметры Yandex Cloud — из консоли (сервисные аккаунты, Object Storage, Foundation Models, Vision OCR).[file:1]


### 3. Залить фронт в Object Storage

1. Создай S3‑бакет и включи статический хостинг.[file:1]
2. Залей `webapp/index.html/index.html` как `index.html` в корень сайта бакета.[file:1]
3. Убедись, что `https://<YC_WEBSITE_HOST>/index.html` открывается в браузере.[file:1]

### 4. Запустить бота

```bash
python -m main
```

`main.py` создаёт `Bot`, `Dispatcher`, подключает все роутеры и запускает polling.[file:1]

## Как это работает

### Пользовательский сценарий

1. Пользователь отправляет команду `/start` и видит главное меню с кнопками:
    - «📄 Создать карту»,
    - «📚 История»,
    - «🌐 Открыть мини-приложение» (если есть последняя карта, открывает её).[file:1]
2. При выборе «Создать карту» бот просит загрузить файл/фото, затем:
    - запросить название карты;
    - запросить глубину анализа (Кратко / Средне / Подробно);
    - запросить модель LLM (сейчас один вариант «Авто»).[file:1]
3. После ввода параметров бот:
    - вытаскивает текст из файла/фото;
    - отправляет текст в LLM;
    - получает JSON‑структуру документа;
    - генерирует Markdown для Markmap;
    - сохраняет Markdown в Object Storage;
    - сохраняет запись о карте в памяти;
    - присылает структуру и кнопки для открытия карты.[file:1]

### Извлечение текста (`services/document_text.py`)

- Фото (`message.photo`):
    - скачиваются байты фото из Telegram;
    - вызывается Yandex OCR (`mimeType="JPEG"`, `languageCodes=["ru","en"]`, `model="page"`);[file:1]
    - из ответа берётся `fullText` или текст из `blocks/lines`.[file:1]
- Документы (`message.document`):
    - файл скачивается из Telegram;
    - по расширению выбирается стратегия:
        - `.txt`/`.md`/`.csv` — читается как текст с диска;
        - `.docx` — парсится через `python-docx`;
        - `.pptx` — парсится через `python-pptx`;
        - `.pdf` — сначала `pypdf` (если PDF текстовый), если текста нет или произошла ошибка — запрос в OCR как `mimeType="PDF"`;
        - другие форматы — попытка прочитать как текст и, если не получилось, fallback на OCR как изображение.[web:5][web:8][file:1]

Возвращаемый текст отправляется дальше в LLM.

### Генерация структуры и mindmap (`services/llm.py`)

- Формируется `SYSTEM_PROMPT`, который задаёт формат ответа строго в виде JSON:

```json
{
  "title": "Краткое название карты",
  "nodes": [
    {
      "title": "Краткий заголовок узла",
      "children": [
        {
          "title": "Подузел",
          "children": []
        }
      ]
    }
  ]
}
```

- В зависимости от глубины (`Кратко/Средне/Подробно`) подставляется текст‑подсказка из `DEPTH_HINTS`.[file:1]
- Запрос к Yandex LLM (`YANDEX_API_URL`, `modelUri=YANDEX_URL`) отправляется с `jsonObject=True`; из ответа берётся строка JSON.[file:1]
- JSON парсится в Python‑объект, далее:
    - строится плоский список `flat` для вывода в Telegram (с отступами и `-`);
    - собирается Markdown для Markmap (`# title`, далее `-` с вложенностью).[file:1]
- Если ответ пустой/битый, используется fallback‑структура с базовыми узлами «Введение / Ключевые идеи / Основные пункты / Выводы».[file:1]


### Сохранение карты и ссылки (`services/storage.py`, `webapp/index.html`)

- `upload_to_s3(md_content, filename)`:
    - создаёт S3‑клиент через `boto3` с `YC_ACCESS_KEY_ID`, `YC_SECRET_ACCESS_KEY`, `YC_S3_ENDPOINT`;[file:1]
    - кладёт файл `generated_maps/<uuid>.md` в бакет `YC_BUCKET_NAME` с типом `text/markdown`;
    - возвращает URL вида `http://{YC_WEBSITE_HOST}/index.html?file=generated_maps/<uuid>.md` (дальше он нормализуется до https).[file:1]
- `save_map(user_id, title, depth, structure, markmap, url)`:
    - создаёт `map_id`,
    - кладёт запись в in‑memory `MAP_STORAGE[user_id]`.[file:1]
- Mini‑app (`webapp/index.html`):
    - читает параметр `file` из URL (`?file=generated_maps/uuid.md`);
    - делает `fetch('/' + filePath)` по относительному пути в том же бакете;
    - при 403/404 выводит понятную ошибку и кнопку «Повторить»;
    - при успехе:
        - читает Markdown;
        - создаёт `<svg id="mindmap-svg">`;
        - через `markmap-lib` и `markmap-view` строит mindmap;
        - интегрирован с Telegram WebApp API (`ready()`, `expand()`).[file:1]


### История и просмотр в чате

- «📚 История»:
    - берёт все карты пользователя через `get_user_maps(user_id)`;[file:1]
    - если пусто — пишет «История пуста»;
    - иначе формирует inline‑клавиатуру:
        - кнопка «🌐 {title}» — открывает mini‑app по URL карты;
        - кнопка «👁 Текст» — отправляет плоскую структуру карты в чат через `_flatten_nodes`.[file:1]
- `/cancel`, `/menu`, `/start`:
    - очищают FSM‑состояние;
    - подставляют ссылку на последнюю карту в клавиатуру;
    - возвращают пользователя в главное меню.[file:1]


## Планы по улучшению

- Перенести хранение карт из памяти (`MAP_STORAGE`) в базу данных (SQLite/PostgreSQL).
- Удалить неиспользуемые заглушки и старый код, держать только актуальные сервисы.
- Вынести логику генерации URL в отдельный модуль (одна точка правды для ссылок mini‑app).
- Добавить поддержку дополнительных форматов (например, XLSX) и более гибкую настройку глубины анализа.[file:1]


## Безопасность и секреты

- Не коммить `.env` в публичный репозиторий.
- Никогда не публикуй реальные токены Telegram и ключи Yandex Cloud.
- Для продакшена вынеси токены в переменные окружения инфраструктуры (Docker, облако, CI/CD).[file:1]

```
```

